# 1.5.0 验证记录

验证日期：2026-09-06。候选版本：Minecraft 1.21.1 / NeoForge 21.1.249 / Java 21。

JAR：`royale-spells-neoforge-1.21.1-1.5.0.jar`，SHA-256：

```text
5d7cc596b059fcaf68449d6b3783f04870114c72ef0bc87ceec8b8a74dd31e8a
```

## 构建与服务器回归

| 检查 | 结果 |
| --- | --- |
| Gradle build | 通过 |
| 不安装铁魔法的 GameTests | 40 / 40，通过 |
| 铁魔法 3.16.3 与依赖同装的 GameTests | 91 / 91，通过 |
| JAR ZIP 完整性、版本、注册类和资源 | 通过 |
| 新增 7 个 OGG 的哈希与单声道解码 | 通过 |
| 原有 55 个法术阶段事件的 64 格衰减设置 | 通过 |
| 被否定的圆润骷髅模型 JSON 未打入 JAR | 通过 |

执行 `gradlew.bat build`、`gradlew.bat runGameTestServer` 和 `gradlew.bat runCompatGameTest -PcompatModsDir=../neoforge-reference/compat-mods`。

XML 报告在 `validation/run-gametest-results.xml` 和 `validation/run-compat-gametest-results.xml`；其中没有 failure/error。服务器检查完成后只改进了 opt-in 客户端拍摄与动画采样。打包脚本逐项比较两个 JAR，确认唯一不同条目为 `ArmyClientSmoke.class`，生产逻辑、素材及服务器测试类没有变化。

新增测试覆盖任意等级输入、错误卷轴拒绝、天然标记及桶搬运、掉落物实际启动仪式、存储／中断返还、仅觉醒卷轴可附魔号角、名称保留、副手零魔力号角、法术书扣费、卷轴独立施放、双方共享冷却与军团上限、不同玩家各持一支、护盾不溢出伤害、幽灵转换及将军死亡、区块卸载／存档保留记录、蓄力后单次伤害。虚空检查实际计时 16／40／64 tick 各触发一次声音。既有测试同时回归抄写、提取、卷轴制作过滤、数值、液池结构四种朝向与原容器保留。

## 使用最终 JAR 的独立客户端

真实启动 NeoForge 生产客户端并载入铁魔法及依赖，使用独立目录 `run-production-army-1_5_0-final-c` 和测试世界。客户端 mods 中的 JAR 与交付文件哈希一致。启动、进入世界、执行下列检查并正常退出；没有使用玩家世界。

- 原生火术师塔生成 8 个结构片段，深层重油源坐标为 `(725, 42, 160)`，比旧液池位置再深 8 格。
- 实际投入墓园卷轴，产出原生觉醒军团卷轴；再投入卷轴和山羊角，完成融合。
- 客户端调用实际物品使用接口吹响号角，服务器生成 16 个成员；主动击败普通成员，确认转为幽灵。
- 在 0 魔力副手测试中验证号角不扣魔力；图形客户端初始仅 1 魔力也成功施放，随后显示的增长来自正常魔力恢复。
- 查看原版骨架的小骷髅、将军头盔／旗帜／夜间眼睛、铁魔法原生召唤骷髅持弓画面。
- 客户端接收将军攻击进度，采到 9 个实际攻击帧，最高进度 0.944；画面覆盖蓄力、前刺和收回，并断言目标受到伤害。
- 移除将军后确认余下军团全部消散。
- 站在虚空中心 **40 格**外，客户端 SoundEngine 接收到 **3 次**劈击，衰减距离 64、播放增益 0.63。
- 在 **56 格**外接收到火球命中音效，衰减距离 64、播放增益 0.765。

完整日志在 `validation/console.log`；通过标记包括 `ROYALE_ARMY_STRIKE_ANIMATION_VERIFIED`、`ROYALE_VOID_THREE_DISTANT_IMPACTS_VERIFIED`、`ROYALE_SPELL_AUDIO_56_BLOCKS_VERIFIED` 与 `ROYALE_ARMY_CLIENT_COMPLETE`。共 18 张未修改截图，其中 9 张为隔 2 tick 采样的攻击帧；预览视频只把这些帧按采样间隔编码，不是额外生成的动作。

## 验证边界

本轮验证了上述服务端规则和独立生产客户端流程，未替换用户整合包，也未进行真实双客户端联机、光影／第三方渲染器兼容或长期平衡试玩。世界记录的存取与实体卸载通过测试；没有把它描述成手工完成了全部跨维度／重登场景。军团沿用既有铁魔法召唤／驱散接口，未单独增加一次军团反制的图形客户端测试。

音频文件可解码、触发时点和远距离客户端接收均已验证；这不代表代替用户在实际音箱／耳机上试听并确认响度偏好。虚空劈击明确复用原作电击素材，并未声称找到其单独的专属音轨。

铁魔法自身的 `template_open_spell_book_model` 缺失模型警告仍出现在该依赖组合启动日志中；本轮没有新增对应警告或客户端错误，原生卷轴／持弓／军团流程已完成。

1.4.0 旧塔没有本版天然源标记，新仪式需要新生成的塔；不会自动挖改已探索结构。旧版输出和玩家存档保持原样。
