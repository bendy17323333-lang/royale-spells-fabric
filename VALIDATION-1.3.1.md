# 1.3.1 落点预览修复

2026-09-06，本地修复版；未推送或发布 GitHub。

## 修复内容

1.3.0 将中心十字和外圈的每个顶点分别投射到地面。跨台阶、树冠时，同一个图形因此出现数格高差，产生竖线、尖角和明显拉伸。

1.3.1 的圆形和滚动轨迹在各自统一的水平面上绘制。半径与施法范围一致；觉醒电击仍有内外两圈，觉醒飞桶仍有两个独立落点。被地形遮挡的线段显示较淡，整圈保持可读。中心改为跟随实际落点投影的固定尺寸四角标记，不随距离、地形或视角拉伸。

地面范围仍遵循正常三维透视，因此远处在屏幕上更小、斜看呈椭圆；固定屏幕尺寸的是中心标记。两层轮廓仅写入颜色，不写入场景深度。

## 最终 JAR

- `royale-spells-neoforge-1.21.1-1.3.1.jar`
- SHA-256：`59daf4663ed6e4b182183dbd9270f562f85de8779cc41dae818c7fd98b9b7616`
- Java 21.0.12 / Minecraft 1.21.1 / NeoForge 21.1.249。
- `gradlew.bat build` 通过，ZIP 完整性与版本元数据通过。
- 与交付的 1.3.0 JAR 逐项比较：仅客户端预览、渲染层、可选测试入口及版本元数据不同。法术机制和铁魔法集成的类与资源逐字节一致。

## 实际客户端检查

三个隔离新存档客户端加载同一最终 JAR，均输出完成标记并正常退出：

| 组合 | 运行目录 | 场景数 |
|---|---|---:|
| 铁魔法完整组合，Fancy | `run-production-preview-compat-1_3_1-terrain` | 14 |
| 铁魔法完整组合，Fabulous | `run-production-preview-compat-1_3_1-terrain-fabulous` | 14 |
| 未装铁魔法，Fancy | `run-production-preview-1_3_1-terrain-standalone` | 14 |

每轮覆盖台阶近景／远景／左右观察、分层树冠近景／远景／侧视、FOV 100、第三人称、普通小电、觉醒小电、觉醒飞桶双落点、跨台阶滚木和滚桶。检查 F1 隐藏界面、切换空手时没有残留预览。

- 实际提交渲染的每个轮廓顶点高度一致，圆形水平半径与法术定义一致。
- 每轮 13 个中心标记从原始截图中测量，在 GUI 缩放 2 时均为 **26 × 26 物理像素**，包括远近场景和双落点；测量结果见 `validation/*-pixels.json`。
- 每个场景绘制前后对整幅 1600 × 1000 深度缓冲逐像素比较，均保持一致，每轮 14 次。
- 原始截图见 `预览.html` 和 `previews/`，未裁切、重绘或合成。已查看 Fancy/Fabulous 的台阶、树冠与滚木画面。

本次针对预览回归，没有重复运行未改动的服务器玩法测试；历史 38 项独立／65 项完整集成结果见 `VALIDATION-1.3.0.md`。不代表所有光影包和第三方整合包均已验证。没有替换用户当前实例或修改已有存档。

## 复现

在已有官方客户端依赖的本地 QA 工作区运行 `qa/launch-iron-integration-client.py preview-compat <新目录后缀>`。后缀包含 `fabulous` 时使用该画质，`preview` 模式不装铁魔法。启动脚本需本地的 `work/neoforge-reference/production-launcher` 和兼容测试依赖目录；这些第三方文件没有打入源码包。

客户端完成后运行 `python tools/validate-target-screenshots.py <运行目录>`，测量截图中的中心标记。脚本只读取原图并输出测量 JSON。
